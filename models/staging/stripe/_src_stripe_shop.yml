sources:
  - name: stripe
    database: dbtprac
    schema: stripe
    tables:
      - name: payment
        freshness:
          warn_after:
            count: 6
            period: hour
          error_after:
            count: 12
            period: hour
        loaded_at_field: _batched_at